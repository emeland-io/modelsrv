# Pet

A pet in the demo catalog (e.g. a dog).

- **Type:** object

## Properties

- `kind` (string) — Species kind. Enum: `dog`, `cat`, `other`.
- `name` (string, required) — Display name.
- `petId` (string (uuid), required) — Unique id.

